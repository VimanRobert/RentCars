@model IEnumerable<RentCars.Models.Car>

@{
    ViewData["Title"] = "Home";
}

<h1>Available cars</h1>
<p>
    <a asp-action="Create">Add new item</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-action="Index" asp-routesortOrder="@ViewData["TitleSortParm"]">@Html.DisplayNameFor(model => model.carId)</a>
            </th>
            <th>
                <a asp-action="Index" asp-routesortOrder="@ViewData["TitleSortParm"]">@Html.DisplayNameFor(model => model.carBrand)</a>
            </th>
            <th>
                <a asp-action="Index" asp-routesortOrder="@ViewData["PriceSortParm"]">@Html.DisplayNameFor(model => model.carPrice)</a>
            </th>
        </tr>
        </thead>
 <tbody>
@* @foreach (var item in Model.Select((value, index) => new { Index = index, Value = value }))
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Value.carId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Value.carBrand)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Value.carPrice)
        </td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.Value.carId">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.Value.carId">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.Value.carId">Delete</a>
        </td>
    </tr>
} *@
 </tbody
